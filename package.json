{
  "name": "project1",
  "version": "1.0.0",
  "description": "my first code",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "ganesh",
  "license": "ISC",
  "dependencies": {
    "favicon": "0.0.2"
  },

  "engines": {
    "node": "4.1.1"
  }
}
